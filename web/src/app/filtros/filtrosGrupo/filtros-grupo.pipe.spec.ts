import { FiltrosGrupoPipe } from './filtros-grupo.pipe';

describe('FiltrosGrupoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrosGrupoPipe();
    expect(pipe).toBeTruthy();
  });
});
