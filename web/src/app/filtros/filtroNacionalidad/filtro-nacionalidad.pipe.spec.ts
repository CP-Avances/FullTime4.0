import { FiltroNacionalidadPipe } from "./filtro-nacionalidad.pipe";

describe('FiltroNacionalidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNacionalidadPipe();
    expect(pipe).toBeTruthy();
  });
});
