import { SucRolEmpleadoPipe } from './suc-rol-empleado.pipe';

describe('SucRolEmpleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new SucRolEmpleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
