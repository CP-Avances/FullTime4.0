import { SucNomEmpleadoPipe } from './suc-nom-empleado.pipe';

describe('SucNomEmpleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new SucNomEmpleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
