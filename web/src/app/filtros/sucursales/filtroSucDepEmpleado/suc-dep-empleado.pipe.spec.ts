import { SucDepEmpleadoPipe } from './suc-dep-empleado.pipe';

describe('SucDepEmpleadoPipe', () => {
  it('create an instance', () => {
    const pipe = new SucDepEmpleadoPipe();
    expect(pipe).toBeTruthy();
  });
});
