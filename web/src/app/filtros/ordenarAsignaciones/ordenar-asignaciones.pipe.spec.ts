import { OrdenarAsignacionesPipe } from './ordenar-asignaciones.pipe';

describe('OrdenarAsignacionesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarAsignacionesPipe();
    expect(pipe).toBeTruthy();
  });
});
