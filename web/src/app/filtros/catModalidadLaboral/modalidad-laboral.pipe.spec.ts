import { ModalidadLaboralPipe } from './modalidad-laboral.pipe';

describe('ModalidadLaboralPipe', () => {
  it('create an instance', () => {
    const pipe = new ModalidadLaboralPipe();
    expect(pipe).toBeTruthy();
  });
});
