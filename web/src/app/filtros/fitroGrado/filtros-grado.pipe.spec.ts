import { FiltrosGradoPipe } from './filtros-grado.pipe';

describe('FiltrosGradoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrosGradoPipe();
    expect(pipe).toBeTruthy();
  });
});
