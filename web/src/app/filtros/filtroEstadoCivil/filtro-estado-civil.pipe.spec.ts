import { FiltroEstadoCivilPipe } from './filtro-estado-civil.pipe';

describe('FiltroEstadoCivilPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEstadoCivilPipe();
    expect(pipe).toBeTruthy();
  });
});
